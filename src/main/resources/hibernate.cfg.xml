<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
 "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
 "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- Conexão com o PostgreSQL -->
        <property name="hibernate.connection.driver_class">org.postgresql.Driver</property>
        <property name="hibernate.connection.url">jdbc:postgresql://localhost:5432/GerenciaEntrevista</property>
        <property name="hibernate.connection.username">admin</property>
        <property name="hibernate.connection.password">9918</property>

        <!-- Dialeto do PostgreSQL -->
        <property name="hibernate.dialect">org.hibernate.dialect.PostgreSQLDialect</property>

        <!-- Mostrar SQL no console -->
        <property name="hibernate.show_sql">true</property>

        <!-- Atualizar esquema automaticamente -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Classes mapeadas -->
        <mapping class="com.seuprojeto.model.SuaClasse"/>
    </session-factory>
</hibernate-configuration>
